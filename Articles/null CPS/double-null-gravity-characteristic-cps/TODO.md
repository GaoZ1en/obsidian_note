# Immediate Research Tasks

- [x] Action: retain $S_+$ and $S_-$ as area/boost ports and compute the
  endpoint-complete Stage-1 rank/kernel.
  Context: the abstract signed port variables $b_\pm$ keep the closing normal
  data without choosing a particular outer wall.
  Check: the extended matrix has rank six; the fixed-area pullback has rank two
  with only the unused $b_\pm$ source directions in its kernel.

- [ ] Action: run the cross-focusing constraint on the shear-free/twist-free
  ansatz.
  Context: this is a preservation/closure test for the special spin-0
  truncation, **not** an extra algebraic corner compatibility condition on
  generic CK/Sachs characteristic data.
  Check: determine which transverse derivatives regenerate shear or twist and
  state the additional restrictions, if any, only for this truncation.

- [ ] Action: perform a genuine Stage-2 pullback of the conformal-metric
  Dirichlet term, using the corrected bare-measure density
  $\Omega\widetilde\sigma^{AB}$ and imposing
  $\bar q^+|_{S_0}=\bar q^-|_{S_0}$ once.
  Context: calculation 02 now fixes the kinematical density convention but does
  not reduce the spin-2 constraints.
  Check: derive the nonlocal reduced form and compare with Reisenberger only as
  a convention, gauge, and endpoint regression.

- [ ] Action: realize the abstract $b_\pm$ ports with one concrete oriented
  closing geometry if a geometric boundary action is needed.
  Context: Stage 1 is already closed as an extended port system; this step
  identifies the port momenta with explicit outer normals/embeddings.
  Check: reproduce (5.3) up to the declared endpoint canonical transformation.

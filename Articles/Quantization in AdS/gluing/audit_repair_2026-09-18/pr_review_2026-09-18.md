# PR #10 本地复核与补充修复

复核对象：[PR #10](https://github.com/GaoZ1en/obsidian_note/pull/10)，初始 head `7ade529c3d88814eff2b23424830d4f89a6e40d3`，共同基线 `6940ab5243b7d8f74bcfc2f2cf0138eeed50b240`。日期：2026-09-18。

**结论：在正文列明的有限切分、固定物理边界和模型条件下，可以接受修订后的联合源与未约化 presymplectic sewing 表述。** 通用 comparison principle 仍是带条件的 relation 比较；存在性、唯一性、满射性要由相应模型的构造证明，不能由有限回测代替。输入审查表也没有证明每一条输入都是逻辑上独立、不可弱化的最小公理。

## 本轮发现并修复的问题

| 问题 | 原先失败的方式 | 修复后的保证与范围 |
|---|---|---|
| 合稿同步不幂等 | append-only 的新源码包含旧源码，第二次同步再次追加同一段 | 识别唯一的新块，拒绝额外的旧块或新块；加入追加、删减和重复块测试 |
| 合稿中的相对链接错层 | 分篇中的 `../../audit_repair_2026-09-18/` 原样复制到包根目录，指向错误位置 | 按分篇和合稿目录重定位本文使用的 inline Markdown 链接；外部 URL、绝对路径和锚点保持不变 |
| 模型章仍有提前约化的措辞 | 主定理保留全部场与 proper gauge，模型章却把代表选择写成删去 bulk extension 或规范纤维 | PB 第 05–07 篇明确区分完整场对象与派生坐标、可观测量和表示；规范变换作为实际作用保留。固定 bundle presentation 的 restriction/assembly 才互逆，改变 presentation 必须携带实际 bundle map |
| Maxwell 的光滑性论证缺少输入 | 把任意无限 collar jets 的延拓存在性直接用于宣称可微的 `J` 和切映射满射 | R4.3 明确使用实际的光滑 collar representatives、固定互不相交 collars 和 cutoffs；在这一空间中构造连续线性装配映射及 restriction section。仅给抽象 jets 时，不宣称已经证明连续线性 Borel section |

两份阅读合稿均已同步：FC 更新 3 个源码块，PB 更新 8 个源码块；再次运行同步工具，两包均为 **0 个块变更**。历史 counterexample/input audits、原 manifest、原 ZIP 和初次 Pro 回测的 `checks_result.json` 均未改写。同步工具只预检本次变化的源码块；不认证合稿中所有未变文字的历史哈希。

20 份本次变动的 Markdown 均通过 Pandoc 解析，未发现未解析的相对文件链接；FC 全部 6 篇、PB 全部 12 篇分篇均在对应合稿中逐块唯一匹配（链接按合稿目录重定位后比较）。`git diff --check` 与 vault policy audit 通过。这些文档检查不检验数学命题真值。

## 实际执行的检查

| 检查 | 结果 | 证据能够支持的范围 |
|---|---|---|
| `verify_repair.py` | 63/63 通过 | 本修订列出的有限符号恒等式、线性模型和反例机制 |
| FC `checks/verify.py` | 86/86 通过 | 原标量包实现的解析/数值例子；不独立证明一般 PDE 定理 |
| PB `checks/run_checks.py` | 56/56 通过 | 原主丛包实现的有限代数、模型与正性检查 |
| `test_sync_reading_copies.py` | 12/12 通过 | 追加/删减的幂等性、异常块拒绝、链接重定位、dry run 和两包写入前共同预检 |
| Mathematica 精确检查 | 所列残差为零 | 联合源例子、振子 `Theta` 的精确变化、seam gauge 方向与二形式、三项 overlap 核、quartic Wick 转换，以及一个非阿贝尔 YM2 解的曲率、Gauss 和时间演化方程 |
| Sage 精确检查 | 6/6 通过 | `M_2` 张成、固定 cups 下的符号失败、Pauli 中心子反例、伴随作用交换性、等距嵌入不保证投影保持像、相同边际的两个纯 Gaussian 例子 |

Python 检查运行在独立临时环境：Python **3.14.7**、SymPy **1.14.0**、NumPy **2.4.4**、SciPy **1.17.1**、Shapely **2.1.2**、mpmath **1.3.0**。这不是原包记录的精确依赖版本复刻；未更改仓库依赖声明。Sage 版本为 **10.9**。FC 初次启动因缺少 Shapely 中止，补齐该临时环境依赖后完整重跑得到上述 86/86；失败启动不计作通过。

本轮未修改 `verify_repair.py`，其 SHA-256 为 `bf461913265ce2b87bbcbbf8a876171084e3e513ff8ff733c37270ff3273d817`。该脚本的输出只描述它自己执行的检查；原 suites 与 CAS 的实际执行范围以上表分别记录。仓库内初次 `checks_result.json` 继续记录 Python 3.13.5 的历史执行，不能与本次重跑环境混写。

在具备上述依赖的 checkout 中，从仓库根目录可分别复跑：

```bash
python3 "Articles/Quantization in AdS/gluing/audit_repair_2026-09-18/verify_repair.py" --output /tmp/sewing-repair-recheck.json
python3 "Articles/Quantization in AdS/gluing/finite_cut_sewing/checks/verify.py"
python3 "Articles/Quantization in AdS/gluing/principal_bundle_sewing_v2/checks/run_checks.py"
python3 "Articles/Quantization in AdS/gluing/audit_repair_2026-09-18/test_sync_reading_copies.py"
python3 "Articles/Quantization in AdS/gluing/audit_repair_2026-09-18/sync_reading_copies.py"
```

最后一条默认只预检。实际同步需显式 `--write`；两包都通过预检后才开始写入。原 suites 可能生成各自的运行附件，不应将其与历史记录混同。

**Verified:** 上表各项已实际执行。counterexample 检查通过的含义是重现被攻击命题的失败，不是证明该命题正确。

**Assumptions:** 沿用 active notes 的 action、法向与 bracket 符号、真实边界条件和全局 proper 分支策略；保留未约化场、退化方向及其实际规范作用。Maxwell 的 `J` 与 `dJ` 结论采用 R4.3 中明确的实际光滑 collar 数据空间。

**Not verified:** 未用 xAct 认证一般张量或 Noether 恒等式；没有从这些有限检查推出任意几何上的 PDE 存在性、所有 source-dependent Ward identities、无 regulator 的连续量子代数、thin Wilson/ribbon 正则化独立性，或全部输入的普遍最小性。它们继续按正文的条件性或开放问题处理。

a simple code
```python
print("hello")
```

## list, tuple and dictionary

```python
listName = []
listName.append(variable) # add a variable
listName.index(variable) # return the index of a specific variable
```

slice of list, somehow return sublist

```python
listName[n:m]
listName[:m]
listName[n:]
```

copy of list. notice the difference between `listName2 = listName` and `listName2 = listName.copy()`. the former one will change the original list when you change the copied list, and the latter one will not. so 
## pre-lecture preparation

download docker and pull the image ningsu/simpleboot:4.0:

```bash
docker pull ningsu/simpleboot:4.0

```

then run the container:

```bash
docker run -it --rm ningsu/simpleboot:4.0
```

## ???

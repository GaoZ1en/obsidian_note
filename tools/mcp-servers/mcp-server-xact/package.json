{
  "name": "mcp-server-xact",
  "version": "0.2.1",
  "description": "A Model Context Protocol server for xAct-backed Wolfram Language checks",
  "type": "module",
  "bin": {
    "mcp-server-xact": "./build/index.js"
  },
  "scripts": {
    "build": "tsc && chmod 755 build/index.js",
    "test": "npm run build && node --test test/*.test.mjs",
    "prepare": "npm run build"
  },
  "files": [
    "build"
  ],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "typescript": "^5.6.2"
  }
}

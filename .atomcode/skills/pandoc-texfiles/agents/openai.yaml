interface:
  display_name: "Pandoc Texfiles"
  short_description: "Convert Markdown articles into TeX fragments"
  default_prompt: "Use $pandoc-texfiles to add a Markdown-to-TeX fragment workflow for this article draft."

policy:
  allow_implicit_invocation: true
